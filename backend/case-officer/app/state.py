"""CaseState / Evidence / RequirementItem Pydantic models.

PLACEHOLDER — implemented in Phase B step 7 (implementation plan Phase 1,
schema given verbatim in implementation plan section 1).

Note: ``frontend/src/types/caseState.ts`` is a hand-written mirror of this
module. Keep the two in sync — that is what makes Phase C a swap rather than
a rewrite (implementation plan section 5).
"""
